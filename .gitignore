# Dependencies
node_modules
.pnp
.pnp.js

# Testing
coverage

# Production
dist

# NOTE: the compressed 3D models in public/models/ AND the manifest
# models-optimize.cache.json are intentionally COMMITTED, not ignored — that's
# what lets the optimizer compress each model once and skip it on every later
# build (see docs/asset-pipeline.md). Raw sources live in models-src/.

# Misc
.DS_Store
*.pem

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Environment
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Editor
.vscode/*
!.vscode/extensions.json
.idea
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
